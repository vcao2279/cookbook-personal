# import * from './generated/prisma.graphql'
type Mutation {
  createRecipe(
    title: String
    image: String
    readyInMinutes: Int
    servings: Int
  ): Recipe!
}

type Query {
  recipes: [Recipe]!
}

type User {
  id: ID!
  name: String!
  email: String!
  # recipes: [Recipe!]!
}
